import CreateProjectForm from "@/components/forms/proect-create-form";

const CreateProjectPage = () => {
   return (
      <div className="page flex items-center justify-center">
         <CreateProjectForm />
      </div>
   );
};

export default CreateProjectPage;
